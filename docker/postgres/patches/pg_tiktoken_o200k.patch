diff --git a/Cargo.toml b/Cargo.toml
index ab459ba..ef2af60 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -25 +25 @@ pgrx = { version = "=0.16.1", features = [ "unsafe-postgres" ] }
-tiktoken-rs = { git = "https://github.com/kelvich/tiktoken-rs" }
+tiktoken-rs = { git = "https://github.com/zurawiki/tiktoken-rs", rev = "a79050e5a465592dc7e80f23ed69992bb1ac7f50", package = "tiktoken-rs" }
diff --git a/src/lib.rs b/src/lib.rs
index 4551d55..ca3e087 100644
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -3,5 +3,4 @@ use pgrx::prelude::*;
-use tiktoken_rs::encoding_for_model;
-use tiktoken_rs::r50k_base;
-use tiktoken_rs::cl100k_base;
-use tiktoken_rs::p50k_base;
-use tiktoken_rs::p50k_edit;
+use tiktoken_rs::{
+    cl100k_base_singleton, get_bpe_from_model, o200k_base_singleton,
+    p50k_base_singleton, p50k_edit_singleton, r50k_base_singleton,
+};
@@ -16,13 +15,12 @@ pgrx::pg_module_magic!();
-fn encode_with_model(encoding_selector: &str, text: &str) -> Vec<usize> {
-    let encoder_name = encoding_for_model(encoding_selector)
-        .unwrap_or(encoding_selector);
-
-    let encoder = match encoder_name {
-        "cl100k_base" => cl100k_base(),
-        "r50k_base" | "gpt2" => r50k_base(),
-        "p50k_base" => p50k_base(),
-        "p50k_edit" => p50k_edit(),
-        _ => error!("'{encoding_selector}': unknown model or encoder")
-    }.unwrap();
-
-    encoder.encode_with_special_tokens(text)
+fn encode_with_model(encoding_selector: &str, text: &str) -> Vec<u32> {
+    let bpe = match encoding_selector {
+        "o200k_base" => o200k_base_singleton(),
+        "cl100k_base" => cl100k_base_singleton(),
+        "r50k_base" | "gpt2" => r50k_base_singleton(),
+        "p50k_base" => p50k_base_singleton(),
+        "p50k_edit" => p50k_edit_singleton(),
+        _ => get_bpe_from_model(encoding_selector)
+            .unwrap_or_else(|_| error!("'{encoding_selector}': unknown model or encoder")),
+    };
+
+    bpe.encode_with_special_tokens(text)
@@ -34,2 +32,2 @@ fn tiktoken_encode(encoding_selector: &str, text: &str) -> Vec<i64> {
-        .iter()
-        .map(|&x| x.try_into().unwrap())
+        .into_iter()
+        .map(i64::from)
